import pop_implementation.PopClient;
import user_interface.MainWindow;

public class Main
{
    public static void main(String[] args)
    {
        MainWindow mainWindow = new MainWindow(new PopClient());
    }
}
